spring:
  application:
    name: cloud-gateway-zuul
server:
  port: 8888

zuul:
  routes:
    api-a:
      path: /producer/**
      serviceId: service-provider-helloworld

eureka:
  client:
    service-url:
      defaultZone: http://peer3:8002/eureka/,http://peer2:8001/eureka/,http://peer1:8000/eureka/